//dawit
#include <stdio.h>
#include "vectors.h"


int main( int argc, char** argv ) {
 
  Vector a;
  a = makeVector(3.0,4.0,5.0);
  Vector b;
  b = makeVector(4.0,5.0,9.0);
  Vector sum;
  sum  = add(a,b);
  Vector diffe;
  diffe = diff(a,b);
  double dotProd;
  dotProd = dotProduct(a,b);
  double magA;
  magA = magnitude(a);
  double magB;
  magB  = magnitude (b);
  
  //printf("vector a: ",);
  printVector(a);
  //printf("%s/n");
  return 0; 

  

} // main( int, char** )
